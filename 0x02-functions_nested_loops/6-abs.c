#include "main.h"

/**
* _abs - function that computes the absolute value of an integer
*
* @n:  is the int that will use for the argument of the function
*
* Return: depends on the int
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
